import styled from 'styled-components';

import theme from '@/theme';

export const ClientsLogosContainer = styled.section`
  grid-area: logos;
  /* width: 100%; */
  margin: -16px;

  img {
    width: 100%;
  }
`;
